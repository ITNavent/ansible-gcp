dns:
  - hostname: "{{ inventory_hostname }}"
    domain: "navent.biz"
    ip: "{{ gce_private_ip }}"
    ttl: 300
    type: A

# Hostname
hostname_enabled: yes

# Timezone
timezone: US/Eastern
timezone_format: UTC